version: '2.3'
services:
  chia-plotter:
    build:
      context: .
      dockerfile: Dockerfile
    image: compscidr/chia-plotter:dev
    container_name: chia-plotter
    user: "${UID}:${GID}"
    volumes:
      - ~/.ssh:/home/plotuser/.ssh
